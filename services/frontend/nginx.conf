server {
    listen 80;

    # Serve the React SPA — all unknown routes fall back to index.html
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
